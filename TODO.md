# 功能规划与待实现需求

## 已实现功能

### 1. 一键复制按钮

复制全部已添加文件内容，复制格式如下：

```
---------------
文件路径加文件名称
---------------
文件内容

---------------
```

即每个文件之间用 `---------------` 分隔。

### 2. 排除同步更新

目录树中的目录结构中如果排除了内容，文件内容页面中的目录结构也会跟着更新排除样式。

### 目录树页面

- 单击选中、双击展开/折叠文件夹
- Ctrl+单击多选、框选
- 右键排除/取消排除文件或文件夹
- 排除文件夹时自动包含其子内容
- 排除后树结构文本实时刷新
- 支持按文件类型过滤显示

### 文件内容页面

- 显示与目录树页面相同的目录结构
- 支持点选、框选、Ctrl+单击多选
- 右键菜单包含两个选项：
  - **📄 添加选择文件**：将选中项（文件或文件夹内的所有文件）添加到文件列表，自动识别编码并显示
  - **📂 添加文件**：打开系统文件选择对话框，添加文件
- 每个文件卡片显示：文件路径、编码格式、文件内容、复制按钮
- 复制按钮从 DOM 直接读取内容，避免转义问题
- 目录树展开/折叠状态在软件生命周期中保持

### 已排除列表面板

- 仅在目录树选中行右侧显示
- 支持点击选择和框选
- 支持清除单个或全部排除项
- 文件和文件夹的清除功能均可用

### 交互规范

- 目录树页面：单击选中、双击展开
- 文件内容页面：单击选中、双击展开、右键添加文件
